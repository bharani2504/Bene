version: '3'
services:
  app:
    build: .
    ports:
      - "8081:8081"
    depends_on:
      - db

  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: 250408
      MYSQL_DATABASE: BENE
    ports:
      - "3306:3306"